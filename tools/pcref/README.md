# PC reference measurements

The PC games' neighbour routine, read off the 100 % walkthrough videos
without touching the game: the PC HUD's top-left thought bubble shows the
neighbour's CURRENT activity (coffee cup, deck chair, watering can …), so
its icon per second is his action sequence — camera-independent, unlike
tracking his sprite (the PC view pans and he is often off-screen).

- `bubble.py <video> <start> <dur> <x0> <y0> <w> <h> <out_prefix> [fps] [T]`
  crops the bubble per frame, clusters the crops greedily (24×24 RGB, L2
  threshold T≈900), writes `<prefix>.json` (t, cluster), `<prefix>_icons.png`
  (one exemplar per cluster) and prints the run-length sequence. Crops used:
  Badinfos 720p Season 1 `10 568 120 70`, Badinfos 720p NFH2 `15 548 150 65`.
- `episodes_s1.json`, `episodes_nfh2.json`: episode start/end in the two
  Badinfos videos (from ffmpeg scene cuts + the episode-selection cards).
- `laps_table.py`: the cluster→activity labels (read by eye from the icon
  sheets), the PC sequences, the lap periods, and the mobile side from the
  port's recordings (`tests/run_tricks.py` state.jsonl: the neighbour's
  `using` items) — writes the detail file behind `docs/PC_LAPS_DETAIL.md`.

The radare2 text listings live in `~/nfh-bench/pcref/r2/` (nfh1_game_text.txt, nfh2_gamelogic_text.txt and, since 2026-09-22, nfh1_gfxengine_text.txt: `r2 -q -e scr.color=0 -e asm.lines=false -e asm.comments=false -c 'aaa; pD 0x9dd3a @ 0x10001000' GFXEngine.dll`). Videos live outside the repo in `~/nfh-bench/pcref` (see its README.txt;
`yt-dlp -4` from pcnew). Results: `docs/PC_LAPS.md`.
- `laps_natural.py [--plans <dir>] [--idle <dir>] [level ...]`: the neighbour's
  natural lap (no trick fired) — writes 28 wait-only plans for the harness,
  then pairs the idle recordings' actions with PC_LAPS_DETAIL's bubble spans
  by name (docs/PC_LAPS.md, "Natural laps"; 214's CaptainWheel bubble is the
  captain's door since 2026-09-23).
- `amounts.py <video> [episode ...]`: the NFH2 trick amounts — the gauge's
  jumps per second, each labelled with the bubble activity of that second
  (PC_LAPS_DETAIL.md) and scaled to the full bar, beside the level's mobile
  AngerAmounts (the overlays in levels/pc).
- `gauge.py <video> <start> <dur>`: the PC NFH2 anger gauge (the bar at the
  left of the 720p frame) as fill % per second — its jumps are the tricks'
  amounts, its plateaus the decay (~0.4 %/s).
- `lives2.py <video> <start> <dur> <x0> <y0> <w> <h> <prefix> [T] [white|red]`: the
  PC NFH2 lives counter (top right) binarized and clustered per second — a
  cluster change is a lost life; used to find the catches in a let's play.
- `thermo.py [video]`: the Season 1 thermometer (the neighbour's anger meter,
  bottom-left): the mercury column at 10 Hz per episode, its full holds and
  full-to-empty durations — of the red part of the column only: the white-hot
  top is not red, so its "full" outlives the true one by ~2 s and its drains
  are short. `thermo_rows.py <episode>…` reads the first non-blue row instead
  and prints, per trick, the seconds the fill stays at 97 %+ and the fall in
  %/s — the 60-tick hold at 12 Hz (5 s) and 0.7 × the level's `angrytime`
  ticks over the visible tube (docs/PC_ROUTINES.md); the PC overlays carry
  the angrytime in ticks as PCAngryTime, and the profile runs the PC rule on
  it (docs/PC_FIDELITY.md §7).
- `thermo_jumps.py <episode>…`: the thermometer's jumps per episode — the second
  of every trick of Badinfos' run (game.exe fires the trick before its
  animation), the fill just before each jump and the gap to the previous one:
  the PC's own chain of a level (E06 in docs/PC_LAPS_DETAIL.md — nine tricks,
  eight bonuses). A jump within ~4 s of the tube's emptying can still be a
  bonus (the bulb holds the bar's bottom ~30 %); the HUD's bonus counter in
  a frame settles it, and in the frames Woody is the blond in blue, the
  neighbour the man in the vest.
- `pc_tricked_actions.py`: the Season 1 TRICKED objects' neighbour actions in
  seconds next to the normal object's, paired by the scripts' `If <tricked>
  of <normal>` steps (docs/PC_ROUTINES.md) — the PC plays the tricked
  object's own action (106's make_foampudding 3.17 s, not make_pudding's
  8.00) where the port stretches the tricked clip to the normal stay
  (docs/PC_FIDELITY.md, the 106 paragraph).
- `fire_sites.py [--fibers]`: every trick-fire step site of game.exe (the
  three-, four- and five-argument steps whose run is fcn.0047bd00) decoded off
  the listing by a stack emulation — name, index, flags, animation, actor —
  and, with `--fibers`, a register-valued index or flags resolved through
  radare2's analysis of the level fiber (the prologue constant of the class's
  `run`): the shout choice and the pay-before-clip sites of docs/PC_ROUTINES.md
  "The fire's tail".
- `trick_branches.py [--json out] [--cases N | --chain N | --dump lo hi] [101 …]`:
  the tricked STAND around every fire site — the level class's case chain
  (routine_order.py's walker: a switch of cases ending in yields) simulated
  with the trick in place (its variant test true, the objects a Switch or the
  repair helper takes out of the world tracked, a test kept in a class byte
  followed, the level's own helpers entered one level deep), cut at the walks
  and the bubbles: the DoActions before the fire, the five-argument step's own
  clip, the actions after it, the repair or clean (fcn.0047ae70 or the
  script's own `repair`), in objects.xml seconds — the neighbour's own actions
  (smokepipe_explosive, riphat, spit …) looked up on the `<actor>` blocks.
  `--cases`/`--chain` print a level's cases and chain, `--dump` a code range
  with its strings. The walk-trigger handlers (the doubletakes, the slips,
  the trap) are no cases: their straight branch is read instead.
- `pc_reactions.py [--write] [106 …]`: the keys per mobile item out of the
  stands — PCShoutIndex/PCShoutSkip (the step's index and flag 2, the
  register-valued ones from `fire_sites.py --fibers`), PCUseSecondsTricked,
  PCFireAt, PCFixSeconds, and the walk-bys', slips' and trap's
  PCSurpriseSeconds/PCSlipSeconds/PCFireBefore — into levels/pc's overlays;
  the item -> PC object pairing is its TABLE (docs/PC_FIDELITY.md, "Season 1
  reactions"), each PC trick on the item that pays it — a station tricked
  through its DependsOn plays the dependency's stand, fire and pay
  (World._pc_trick_item), so 113's pain book and 109's chips carry the keys
  beside their hosts; an item the TABLE names is written whatever its mobile
  score (109's CornChips pays 0 on the mobile). A fixing tool's case
  (`tool`: 110's extinguisher, 111's vacuum) gives the take, the stand at
  the target, the use after the repair or of a sound tool and the give back
  (PCGrabSeconds, PCFixUseSeconds, PCToolUseSeconds, PCReturnSeconds);
  RUNTO marks the objects the class runs to (PCRunTo: the gait set to 2
  before the GoTo — 101/102's antenna, 110's extinguisher, 113's valves)
  and FIXRUN 113's valve stations after the flood and the hot heater (the
  switch alone); 111's machines leave their give to the prime leg.
  `--runs-s2` writes Season 2's RUNTO_S2 (PCRunTo) and RETURN_S2
  (PCTrickReturn: a tricked station whose shout and repair the PC plays
  back at it after a run — 205's nailed skis, `pant` the seconds he stands
  first; 2026-09-24), both in place.
- `gamedata.py <gamedata.bnd> list|cat|grep|tags …`: the PC original's data archive
  (a plain ZIP of XML and TGA: per level `level.xml`, `objects.xml`,
  `tricks.xml`, `anims.xml`, `trigger.xml`; copies in ~/nfh-bench/pcref/pc) —
  the PC side as data. It settled what the videos could only estimate
  (docs/PC_FIDELITY.md §7, "The PC's own data"): Season 1 `quota1` per trick
  = the mobile TrickScore but on 109/111/112; `angrytime` per level = the
  thermometer's drain in 1/20 s; Season 2 `rage` per trick = the mobile
  AngerAmount but on eight items, and the gauge is 80 000 rage long. The
  amounts.py readings (all 1.25× the data) are superseded by it.
- `pc_durations_s2.py [--write] <level ...>`: the Season 2 neighbour's station stays into the overlays as PCUseSeconds per visit — the code's (`lap_model_s2.code_stays`) on its CODE levels (all but 206 since 2026-09-24 — 201's free lap after the tutorial too; a PAIRS entry may give one stay per visit), and on every code level the tricked visit (`code_stays_tricked`, 2026-09-24): its stand -> PCUseSecondsTricked, the SHOUT's level -> PCShout with the repair after it -> PCFixSeconds (0 none) where the step has a SHOUT of a level the walker reads, the second its first named record pays -> PCCreditAt where no clip carries the credit, and the linked trick's variant (both tricks in the scene, or 201's crash_long step) -> PCUseSecondsLinked, PCShoutLinked, PCFixSecondsLinked, PCCreditAtLinked (the item's own record) and PCLinkedPaysAt (the linked trick's own record, 202's bridge_electrify), else the PC videos' bubble spans (docs/PC_LAPS_DETAIL.md, the first lap) less the walk the profile's neighbour takes to each station (the port's idle laps, scratchpad s2_idle_visits.json; since 2026-09-23 walked with the PC's door passes and station runs). Written for every Season 2 level (2026-09-18); the aliases pair PC bubble names with the port's uses in order, a port visit the PC never makes (209's first shoe: the PC does the Taj before the shoes) keeps the mobile length as a leading 0 (214's own since its handshake is the PC's, 2026-09-23); since 2026-09-23 also a station's clips at the PC's ticks (`CLIPS` -> PCClipSeconds: 202's mat, the bar over its seven sleeps, and its swim), a clip held until another role has used an item (`WAITS` -> PCWaitFor: the swim until Olga's sub, then the kid's dive and run ashore) and the tricked use's clip after which the record's action has paid (`CREDIT` -> PCCreditAfter: the shark on the sea's `enter`, checked against objects.xml); since 2026-09-24 a clip may be `('none',)` (the PC plays nothing there: 0 s, skipped) and a wait may hold until the other role's use has begun (`at` start: 210's chair until the Mother's call and her call's job and offer after it — a `then` part `(object, action, actor)` is an action whose job posts a behaviour, to its offer's tick; 205's table until Olga is there, her table released with his play); an item in `KEEP_STAYS` keeps its per-visit stays beside its clips (205's skis: the ride's stay, the put's clip). Since 2026-09-25 (an action's behaviour is posted as its job ends): a clip `('job', object, action, actor, k)` is that action's job and the offer's tick over k clips (210's stands at her chair through her `order`); `BEHAVIOUR_AT` -> PCBehaviourAt, the seconds into a stay at which its behaviour reaches the other actor (205's `talk`, 213's `bull`); `BEHAVIOUR_AT_END` -> PCBehaviourAtEnd, after another actor's use (213's ride's `leave`); `STAYS_CODE` stays per visit read from the script where the video's pairing had split a span (213's controls, its wait none). `--code-keys <level ...>` rewrites the keys read from the code and the data alone (PCClipSeconds, PCWaitFor, PCCreditAfter, PCBehaviourAt(End), the tricked visits' keys, and the stays the code times — code_stays, STAYS_CODE, RUSH — per visit as many as the overlay has) without the idle visits the video pairing needs. Its note in an overlay's source is rewritten in place.
- `pc_durations_others.py [--write] <level ...>`: the other actors' stands (the Mother, Olga …) from the PC data's `time` ticks of their actions, paired per level with the idle runs' stands (NFH_SCRATCH/s2_idle_others.json) in ALIAS, into the overlays as PCUseSecondsRole; and a Mother's bar in her own script (`BARS`: 214's deck chair, the sleep step's 600 ticks) as PCSitSeconds / PCSleepSeconds / PCGetUpSeconds — the chair's `enter`, the bar, the chair's `leave`; and another role's clips at the PC's ticks (`CLIPS_ROLE` -> PCClipSecondsRole: 202's Olga on her mat and at the sub, 207's Mother in the pool and her chair, 210's Mother in her chair — a spec `('ticks', n)` or a bar's ticks less so many over so many clips, `('bar', step, n, less)` — and at her call, 213's Olga on the bull); and another role's clip held (`WAITS_ROLE` -> PCWaitForRole: 210's Mother after her call until he stands at her chair); and an item's own clips while another role uses it hidden (`ITEM_CLIPS` -> PCItemClipSeconds: 205's mat under Olga). The ALIAS note stays only on a level with ALIAS stands (2026-09-24).
- `coins.py <level ...>`: the Season 2 coin tricks of the profile's plan against the PC combinations' `<trick>` records — the rage each pays and the records a compound fires (the amounts of the PCExtraCoin overlays); a record's `time` is a frame of its action, not the credit's moment (the `--write-ticks` carry is withdrawn); `--write-laugh` writes PCLaugh, the records' `laugh` level that picks the PC neighbour's reaction clip (the level's commonest level for an item the pairing does not reach).
- `canon.py <level ...>`: the PC canon per level next to the mobile's data —
  tricks and values, recipes, containers, walk-by triggers, rooms, the
  neighbour's action lengths — with the multiset diffs (docs/PC_FIDELITY.md §7,
  "The canon audit").

- `routine_order.py` — the neighbour's lap of every Season 1 level read from game.exe's compiled level classes (the yield chain of each class's switch, simulated with no trick fired, isObjectPresent followed over level.xml's placement and the switches — 2026-09-23); prints the laps beside the mobile routines. Needs the radare2 listing of game.exe (`~/nfh-bench/pcref/r2/nfh1_game_text.txt`) and `exe/nfh1_globals.json`.
- `routine_order_s2.py` — the Season 2 counterpart: the chain of step functions of each GameLogic.dll level script followed with no trick fired; nine laps close (201, 203, 205, 206, 208, 209, 211, 212, 213), the polling steps of 202/204/207/214 and 210's deck chair stop the rest (`TAILS=1` shows the branches). Needs `~/nfh-bench/pcref/r2/nfh2_gamelogic_text.txt`.

- `lap_model_s2.py [201 ...]` — the Season 2 lap by code and data: the level scripts of GameLogic.dll walked symbolically along the untricked path (presence from level.xml's visible objects and the scripts' own hides, shows and switches; IsVariant, isObjectPresent and name compares evaluated; the zero flag followed; event subscriptions and polls handed over), each step's sequence timed from the data (DoAction `time` or animation frames, hideout enter/leave, the bars' ticks, message elements instant) and the walks between the steps by the code (the GoTo's Dijkstra over the rooms, the door-pass step, the walk step's one axis a tick through fcn.10009177's waypoints — `Geometry`, `walk_ticks`, `lap_estimate`). Closes 203, 206, 208, 209, 211, 212, 213, 214 and 202 (the step object's own byte, IsVariant's null test, a poll's awaited object, another actor's action by its actor's animation and a bar's shown hideout read since 2026-09-23); `code_stays` pairs the parts with the mobile routine items (`PAIRS`), which `pc_durations_s2.py` writes for its CODE levels — and on 210, 205, 207 and 204, whose laps hand over to the co-actors' messages or wait on them, their stations once from the step the lap starts at (`OPEN_LAPS`, `LAP_START`, `LAP_BYTES`, `LAP_PRESENT`: 210's `order` handler 0x1001aecc, Fifi's tickle first; 205's play's successor 0x100251eb with the mat's `talk` armed; 207's dive's successor 0x100164ee with Olga on her mat; 204's gong's `leave` 0x10032f52, after the `gong` his own gong use sends him; 2026-09-24). Since the same day it also reads the wait element (fcn.1000ca24), a step in phases (a first pass that writes its own byte and returns: 205's mat, the `talk` then the wait) and a level's own actor record over the generic one (205's neighbor: `lookaround`), and its anims.xml parser goes tag by tag: an empty `<animation … />` or `<object … />` had taken the next one's frames (205's `putski`, 204's gong and jade dummy, 211's ladder, 209's fakir `spit`). An action's `<translation object="false">` moves the actor (`Data.translation`): the lap's next walk leaves from there (not after a step with a `leave`, whose placement is another object's), and `code_moves` hands the moves a GoTo follows to `pc_walks_s2.py` (`tx`). Since 2026-09-24: `code_moves_tricked` the move after a TRICKED visit (the station's step run again with the tricked variants of its IsVariant pairs shown in the normal ones' place: 207's crayfish splash −154, 214's pistol +95, 209's hot coal a placement, 0 — `txt`); `code_stays_tricked` that visit — the station's step run with the item's trick in the scene (`tricked_presence`: the combine.xml combinations that take the inventory the mobile TrickItem requires, their objects shown and their removed ingredients hidden) where that changes its DoActions: the stand up to its SHOUT (fcn.1000f977 / fcn.1000fede; only the station's own parts where two mobile stations share a step — 212's cliff, 203's toilet), the SHOUT's level (its last parameter, the first push: a constant or a register the walker follows through `xor`, `inc`/`dec`, `push n; pop r` and `mov r, n`), the repair after it and the second its first named trick record pays at (fcn.1000140b credits a record on the tick its `time` equals the action's elapsed count, `Data.tricks`) — per item, with `TRICKED_STEP` (a tricked variant only other steps play: 201's damaged buffet, 0x10029c4a and 0x10029a6c) and `LINKED_STEP` (the linked trick's: 201's crash_long, 0x100297c5); and the linked variant — the step run with both the item's and its mobile LinkedItemTrick's tricks in the scene (`mobile_linked`), where that changes its DoActions: its stand, SHOUT and repair, the item's own record (a record the item-only variant plays) and the linked trick's (the first it does not: `_step_records`); 201's free lap from the rail's look (`LAP_START` 0x10028f86, the trickable puddle shown). The message elements Ef82b (an object hidden, update 0x1000cf2a), Efac4 (an actor's flag, update 0x1000d037) and Ef779 (an object shown, update 0x1000ce9c) are instant (`INSTANT`), and so are the pose element (fcn.10014c5c / fcn.1000de51: the E2f40 after it is the actor's animation set, 1 at once — 207's) and the camera (Ef51a: fcn.1000f51a's flag 8 only for a nonzero last argument, and then it waits only while Woody's mini-game runs, [level+0xc] != 0 — none in a lap). Later on 2026-09-24: the tricked run starts from the scene the lap's walk has at the row (`lap_state`, `walk(..., snaps=)`: 209's shoe mat with his shoes on it); the flow past a station's step that has no SHOUT of its own is read where it goes (`TRICKED_CONT`: 'steps' — the continuation the step hands to, run in the scene after it, 204's gong 0x10032f52, 205's skis 0x10024fc2, 211's sweets 0x10030dc2 / 0x10030d0f / 0x10030b9d with `CONT_SCENE`'s objects; 'fight' — Olga's or the Mother's run to him and the generic `fight`, fcn.1000eb19, then the handler's step, 204's rickshaw 0x10032b6f, 207's shell 0x1001596a, 210's elephant 0x1001a379, 214's shower 0x1003ba90 / bouquet 0x1003b677 / pistol 0x1003b328 -> PCHitSeconds); a station tricked only on another's step (`TRICKED_VIA`: 214's wheel from the opened door, 0x1003af18); 206's rows (`TRICKED_ROWS`: the weights at row 7 with the dumbbell and Olga, the dynamite at 9 with the bag); a station whose trick is another item's (`TRICKED_BY`: 214's shower under the bucket); a flow with no SHOUT anywhere -> PCShout -1; and the linked variant's continuation (`LINKED_CONT`: 207's castle over the hedgehog's towel, 0x1001513f until the destroyed castle shows Olga's `n_lift`, then the billboard -> PCHitSecondsLinked, PCResumeHeadSeconds, PCExtraCoinLinked). `code_moves_tricked` goes per visit on a two-way station (201's soaped puddle, `txt` [200, -100]). `pc_durations_s2.py`'s `RUSH` writes the toilets' stays of 211's sweets run (the wc's `puke`, 40 ticks). Later the same day: a step object's own byte tested or written through the register the step's saved `this` is reloaded into (213's limberwall step: `mov eax, [ebp-0x10]; cmp byte [eax+0x28], bl`, 0x10038ab1 — the bull's charge) is the step's byte like `[esi+x]`'s; the show element built by hand (fcn.10014cf7, its object and room pushed by their globals' addresses) shows its object; Eebbf (the camera back, fcn.1000dc0c's vtable 0x100ab954 on the camera's update) and fcn.1000ff47 (a SET) are instant; `AUX_UPDATE` runs a level's aux-script update on each trick's scene (212: the fed parrot's shit on the ledge, fcn.10034e05); `TRICKED_ARM` / `TRICKED_ARM_LINKED` read 206's pad — the flow from the load to the shot, the Mother's fight, SHOUT and repair, the linked shot's three records (-> PCTrickArm, PCExtraPaysAtLinked) — and the harpoon's own shot (the rubber bear; the take and put visits -> PCTrickFire); `_repair_walk` adds the walk to a repair at another object (211's sign, 203's generator) and gives its hotspot (-> PCFixDepart); `TRICKED_BY` maps 203's microphone to the generator's tights (the PC's only stage trick); a SHOUT with no repair in a step that hands over off the lap takes the next step's repair (203's generator, 212's bench, 213's washing tub); `TRICKED_RUSH` reads 211's rush: the wcright record's tick in the wc's puke (-> PCToiletPaysAt) and Olga's mad and fight after the puke (-> PCHitSeconds; less the puke until 2026-09-25, when an action's behaviour was read as posted at its job's end). `Data.loader_time` gives an action record's time as Loader.dll stores it (time="N", or auto: the longer of the actor's and the object's oneshot animation less one, a loop not counted) and `Data.job_ticks` its DoActions job's ticks to the update that posts its behaviour (the Loader's time + 2; 2026-09-25); `Data.action_ticks` is the action's whole job since then (`job_ticks`; the actor's animation's frames first, loops counted, and a time="N" action's N until then), and `station_ticks` adds each step's own ticks to its first timed part (`step_ticks`: a tick per instant element, and the step's — 2 for a step that walks, the GoTo's done tick and the sequence's (3 until 2026-09-27, a GoTo tick before the walk that the code does not have: its first update makes the walk's first step), 1 for one at the place of the last; a step without a timed part hands them to the next). Since 2026-09-27 the walks are `walk_span` — the GoTo's jobs as the code runs them, each pushed with a first run in the tick the last is done (a movement done in its last step's tick, the pass's run back to the far floor pushed without one) — and the tricked flows are on the lap's clock (`_flow`: the flow's step its start, 2 where its row walks — `LAP_WALKS` —, each instant a tick, a continuation's steps their own starts at ('STEP',) marks; `_step_parts_split`, `_step_records`).
- `pc_walks_s2.py [--write]` — (since 2026-09-24, late: Woody's PCApproach carries `tx`, his own action's translation on the object — 208's chalk_sponge, -45) the Season 2 walk into the overlays: the PC rooms mapped onto the mobile zones (the geometric assignment the door graph confirms), per Transition and back door the PC pass of its door pair per pawn (`PCPass`: the `in` run, the straight movement or the enter + leave clips, the `out` run, and the x of `<actor>_in` and `<actor>_out` — `xi`, `xo`, where the floor stretches end and begin — in px of the PC scene; since 2026-09-27 `nb` where the near door's `<actor>` hotspot splits the `in` run, `ox` the run back along x where the far floor line ends short of `<actor>_out` — `xo` the clamped x —, and `jt` the job boundaries' ticks [`in`, straight, `out`]), per routine station of the neighbour, the Mother and Olga its PC object's hotspot x and height and the PC routes to the role's other stations (`PCApproach`; `STATIONS` pairs the items by the level scripts' GoTo targets — 2026-09-24: 212's ledge the parrot, 209's Taj the shoe mat, 204's jade the jade, 205's rocket the firework; `tx`, per visit where the item has several, the neighbour's move over the station's actions when a GoTo follows them — `lap_model_s2.code_moves`, their `<translation>`s: 205's skiing −400, 209's coal walk +190), per Woody item its PC object's `woody` hotspot (`PCApproach` `Woody`; `WOODY` pairs 204 items by the inventory they give or take), per zone its PC room with the <neighbor> records' door hotspots and costs (`PCRoom`, for the runtime's path finder `world.pc_route`). Its keys are rewritten in place (2026-09-24: the writer had dropped every patch carrying them, and with them the duration tools' keys sharing a patch — 207's, 209's, 212's and 213's stays since the routes of 2026-09-23), its note too. 210's call is the Mother's chair for both roles. A station whose GoTo takes another hotspot of its object per visit has `x` per visit (`VISIT_HOTSPOTS`: 201's puddle, `neighbor` for the slip and `neighborleft` for the left slip, 2026-09-24); `txt` the tricked visit's move (`code_moves_tricked`).
- `pc_tutorial201.py [--write]` — the PC's 201 tutorial data into the overlay (`PCTutorial` on the TutorialScriptCamera, 2026-09-24): the `aux` director's texts by their code names (ship1/strings.xml's `tutorial` strings and the `content` welcome), the four waypoints and Woody's start (level.xml, in the mobile zones by PCRoom), the entry's neighbor_entry and neighbor_shout hotspots (objects.xml), the neighbour's wheeze frames (generic/anims.xml), the open rail's repair ticks, the at-object tolerance of fcn.1000e2bd (GameLogic.dll's dword at 0x100cc814, read from the DLL), the PC objects and inventory names against the mobile items. The runtime's TutorialPC201 runs the steps.
- `pc_minigames.py [--write]` — the Season 2 mini-games into the overlays: PCMinigameTicks (the `time` of the game object's Woody action, objects.xml), PCMinigameLevels (the startlevel and endlevel of its combine.xml combination, the wobble's range), PCMinigameFailed (the behavioractor of its `failed` action: the neighbour's `run` on eleven levels, Olga's `shout` on 203, the tutorial's `aux` on 201, none on 212 and 213) and PCMinigameFailedClip (the clip that actor's behaviour plays first when it is not the neighbour: 203's Olga, shout_chinese 69 frames, then eat_chinese; 2026-09-24; its `ticks` the shout's job to its behaviour's post, 70, 2026-09-25), PCMinigameFailedTicks (the `failed` job's, 10: its behaviour is posted as it ends; 2026-09-25) and PCMinigameLift (the height in level px of Woody's place for the game — the object's `woody` hotspot — above its room's floor line, the field's middle 150 px above it; 2026-09-24). Its keys are rewritten in place.
- `pc_catch_s2.py [--write]` — the Season 2 catch into the overlays: checks generic/trigger.xml's room triggers (the neighbour's and the Mother's `fight` on Woody, `position="room" type="always"`), each catcher station's PC object (the one the level script enters: its neighbor_hideout flag and `enter` action in objects.xml) and that every mask-4 call of GameLogic's flag setter outside the enter and leave steps is placed (and the one flag element of mask 4, 210's chair after the `wakeup`: fcn.1000fac4 at 0x1001902c); writes `PCHideout` per station and role — the span of the PC's flag 4 (the whole use, less the clips from a level step's clear on, back from a `set` clip, or kept `until` a later station's end), in place like the walk's keys (2026-09-24).
- `lap_model.py` — the Season 1 lap by code and data: `LAPS=1 routine_order.py`'s ICON / GOTO / ENTER / LEAVE / ACTION tokens of each level class, walked at the neighbour's `<speed>` records between the objects' hotspots through the doors (standing points, `enter`/`leave` ticks), the actions at their `time` or frames; against the PC video's natural laps (docs/PC_LAPS.md). The door sum (the near `enter` plus the far `leave`) is the engine's: game.exe composes the pair as one step list (fcn.00478030) and the 110 video measures a back door at ~3 s. `-v` lists the legs; `LAP_TOKENS=<file>` caches the walker's output.
- `pc_durations.py [--show]` — the Season 1 station stays: each PC station of `lap_model.py`'s lap paired with the mobile routine item that visits it (PAIRS), into the overlays as PCUseSeconds per visit, written in place beside the trick step's keys pc_reactions.py merges into the same entries. A pair may split one station by its actions over consecutive mobile visits (111's machines: the prime, use and unprime legs are give, wash, get_clothes and give, dry, take). Left to the mobile: 106's bath, 104's shaving chain.
- `routine_order.py` `LAPS=1` — prints each level's lap as tokens (`LAP <level> <n>: ICON x | GOTO obj | ACTION act + obj …`), the helpers of the class (the sofa's sit picker) descended into, the compound GoTo/DoAction helpers labelled (fcn.00479f10 GoTo+enter, fcn.0044ac80 GoTo with leave, fcn.00473e20/ea0 enter/leave, fcn.00479c70 DoAction+wait).
