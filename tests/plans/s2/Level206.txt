# Level206 — TotalTricksCount 6 (winning 4); every S2 TrickScore is 0.
# Routine (Rottweiler2, LoopFromSelectedIndex, ActionSelectedIndex 4):
# DogFifi, DeckChair (Zone04) -> Pillows (Zone03) -> DeckChair | DogFifi
# (Zone04) -> LaunchPad, Harpoon, LaunchPad, Harpoon, LaunchPad (Zone01) ->
# FifiWeightsDrop, Weights, FifiWeightsGrab (Zone02) -> DogFifi (Zone04) ->
# DynamiteBox (Zone03), then round from [4] — actions [0..3] run ONCE, so
# the Pillows (whose ActivateItemTrick arms the DeckChair) are a race
# against his one Pillows visit at ~18 s: `usewith!` skips the gate the way
# a human takes that chance. The Mother (a catcher, GameInfo.cs:222) lives
# on the Zone04 deck chairs; Olga's manager ships Frozen. The level is a
# tutorial (TutorialScriptCameraNFH2206 + the LevelScript): Woody is frozen
# until the neighbour's fourth in-game action, the cushion goes on the
# pillows while he is parked, and the LevelScript's action 3 — Woody hides
# in the Pipe — starts the Mother's pillow sequence that unfreezes him.
# The rabbit is inventory (the FleaBlanket's list is [IT2_Fleablanket,
# IT2_Rabbit]); the Rabbit item is the rabbit's return after the pad's Fix.
# EXCELLENT would need one meter overflow (GameInfo.cs:413-416; Season
# 2's decay, 0.37/s, never pauses): the deck chair's 45 at his [3] (84 s)
# is the only sum that could carry the pad pair (30 + 20 + 15) and the
# weights (30) over 100, and that needs the harpoon and the pad armed
# before his [5] at 105 s — the flea blanket's take waits for the Mother
# until ~115 s and the pair pays a loop later, when the chair's 45 is
# gone: the runner peaks at 77. The dynamite goes last either way.
take ToyBox IT2_Fartbag           # LevelScript action 1 (the take waits out the freeze, ~29 s)
usewith! Pillows IT2_Fartbag      # action 2: ActivateItemTrick -> DeckChair (action [3])
take SportsBag IT2_Rubber
hide Pipe                         # action 3: the Mother's sequence, the neighbour resumes at [4]
take FleaBlanket IT2_Fleablanket  # TrickAfterWoodyUse; hands the rabbit too (InventoryItems[1])
usewith Harpoon IT2_Rubber        # Neutral, RottweilerUseTogglesPrime
usewith LaunchPad IT2_Rabbit      # RequireUnprime toggles (prime [5], use [7], unprime [9]); DependsOn/LinkedItemTrick Harpoon
usewith Weights IT2_Fleablanket   # action [11]
await DeckChair 0                 # the cushion pays on his DeckChair sit
await LaunchPad 0
await Harpoon 0
await Weights 0                   # the overflow
activated Rabbit                  # the LaunchPad's Fix (Item.RabbitBehavior) puts the rabbit back
unlock DentureAdhesive            # Dexterity, DexterityUnlocker IT_NONE (Zone04, the Mother's zone)
take DentureAdhesive IT2_Kukident
usewith DynamiteBox IT2_Kukident  # action [14], the next loop
await DynamiteBox 0
