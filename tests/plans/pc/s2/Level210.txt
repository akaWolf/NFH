# Level210 under the PC profile: the standard plan (all eight coins, 90).
# The shop pays ONE coin per visit (the octopus is accepted only after the
# urchin's coin has paid), so the best single lap is the chair with the
# pylon (20), the basket with its board and the drained pool (50), the
# shop (37) and the elephant (30): ~96 after the 0.37/s decay between his
# visits — the second shop coin comes a lap later. Thirteen orderings
# tried (docs/PC_FIDELITY.md §7); the overflow needs either the PC's
# shorter deck-chair sit (code-driven, not data) or a second coin in the
# lap.
take SeaUrchin IT2_Hedgehog            # Zone02 (KeepFull: repeatable)
usewith TurbanShop IT2_Hedgehog        # Zone02 (SecondRequiredInventory IT2_Oktopus is the alternative)
take SeaUrchin IT2_Hedgehog            # a second urchin
take AlmsBowl IT2_Bone                 # Zone03
usewith DogBasket IT2_Bone             # Zone04 in the Mother's first sleep (RottweilerUseTogglesPrime, ships Primed; LinkedItemTrick -> DivingBoard; ExtraCoin210 with the empty pool)
take CricketBat IT2_Bat                # Zone01 once the neighbour has left the deck chair (~55 s)
take SuntanOil IT2_Sunoil              # Zone01
take OlgaBra IT2_Bra                   # Zone01: OlgaBraBehavior puts it within reach during Olga's shower (OlgaShowerPutBra .. TakeBra: 5-22, 42-58, 79-96 ...)
prime WaterMelons IT2_Bra              # Zone03: the bra's PrimingItem (Item.cs:1548-1572) — the held bra becomes IT2_Bra_sling (PrimedInventoryType)
usewith Elephant IT2_Bat               # Zone03: ActivateItemTrick -> ElephantCricketBat
usewith Pylon IT2_Bra_sling            # Zone01: LinkedItemTrick of the DeckChair — BEFORE the chair: the chair is UseOnce (no UseItemMultipleTimes, so its Fix keeps UseOnce), the pair must be armed together for one sit (Item.cs:2137-2142)
usewith DeckChair IT2_Hedgehog         # Zone01 (LinkedItemTrick -> Pylon)
usewith DivingBoard IT2_Sunoil         # Zone04 — in a later Mother sleep: MotherWakeSleepBehavior's MotherLookLoop -> Invoke ProgressBarDelay 2 s -> ProgressBar.RestoreVariables re-arms the Mother210 bar each lap (MotherWakeSleepBehavior.cs:26-52; sleeps 7-26, 65-84, 100-119 ...)
take FishNet IT2_Brailer               # Zone04 (a later Mother sleep)
unlock ToolBelt IT2_Brailer            # Zone01 dexterity search (unlocker IT2_Brailer) -> IT2_Tongs (RequirePriming, PrimingItem Valve); the tongs land only when the take animation ends, ~1.4 s after the done-pass (SearchItem.OnFinishAnimationCompelted)
prime Valve IT2_Tongs                  # Zone01: the tongs prime at the valve (RemoveInventoryAfterRequirePriming); ObjectToPrimeWhenPrimed: Valve -> ValveWaterPuddle, ToolBelt -> Pool (the empty pool)
take ValveWaterPuddle IT2_Oktopus      # Zone01: the puddle's octopus (the shop's second required)
usewith TurbanShop IT2_Oktopus        # Zone02: the shop's SecondRequiredInventory — its second coin (PC: "use octopus with stall")
await TurbanShop 0
await DogBasket 0
await DivingBoard 0
await DeckChair 0
await Elephant 0
await Pylon 0
await 8                               # the extra coin: DogBasket + DivingBoard tricked with the pool drained (Item.cs:2420-2427, ExtraCoin210) — 8/8, 90 (the payments spread over four laps, no overflow)
